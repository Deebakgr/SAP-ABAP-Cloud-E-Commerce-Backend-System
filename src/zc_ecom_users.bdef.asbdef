projection;
strict ( 2 );
use draft;

define behavior for ZC_ECOM_USERS alias Users
{
  use create;
  use update;
  use delete;

  use action suspendUser;
  use action reactivateUser;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}