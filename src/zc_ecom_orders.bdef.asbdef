projection;
strict ( 2 );
use draft;

define behavior for ZC_ECOM_ORDERS alias Orders
{
  use create;
  use update;
  use delete;

  use action cancelOrder;
  use action confirmOrder;
  use action shipOrder;
  use action setPending;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}