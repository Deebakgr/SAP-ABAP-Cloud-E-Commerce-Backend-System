projection;
strict ( 2 );
use draft;

define behavior for ZC_ECOM_BEHAVIOR alias Behavior
{
  use create;
  use update;
  use delete;

use action setView;
  use action setAddToCart;
  use action setPurchase;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}